function ResourceMaterialTable({ userId, userType }) {
    return (
        <div>
            <h1>Resource Material table for {userType}</h1>
        </div>
    );
}

export default ResourceMaterialTable;
